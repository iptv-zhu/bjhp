<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="376dp"
    android:layout_height="198dp"
    android:layout_gravity="center"
    android:background="@drawable/mytoast_bg"
    android:orientation="vertical"
    android:padding="10dp">

    <ListView
        android:id="@+id/pay_typelist"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:choiceMode="singleChoice"
        android:divider="@drawable/pay_diver"
        android:listSelector="@color/transparent"
        android:paddingLeft="18dp"
        android:paddingRight="18dp"
        android:paddingTop="28dp"></ListView>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="horizontal"
        android:padding="16dp">

        <ImageButton
            android:id="@+id/update_ok"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/vod_plays" />

        <ImageButton
            android:id="@+id/update_cancle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginLeft="60dp"
            android:background="@drawable/cancle_btn" />
    </LinearLayout>


</LinearLayout>